@model IEnumerable<VegeMoteur.App.Database.Cale>

@{
    ViewBag.Title = "Index";
}

<h4>Cale List</h4>

<p>
	<a href="@Url.Action("Create")" class="btn btn-outline-primary"><i class="fas fa-plus-circle"></i> Create New</a>
</p>
<table class="table table-hover display">
<thead>
    <tr>
		<th>
			Valeur
		</th>
        <th>
           Code Norme
        </th>
        <th></th>
    </tr>
</thead>
<tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Valeur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Norme.CodeNorme)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}
</tbody>
</table>
